{
  "name": "wa-campaigns",
  "version": "1.0.0",
  "private": true,
  "description": "WhatsApp campaign dashboard with GOWA backend",
  "workspaces": [
    "server",
    "dashboard"
  ],
  "scripts": {
    "dev": "concurrently -n server,dashboard -c blue,green \"npm run dev -w server\" \"npm run dev -w dashboard\"",
    "build": "npm run build -w dashboard && npm run build -w server",
    "start": "npm run start -w server",
    "dashboard:dev": "npm run dev -w dashboard",
    "server:dev": "npm run dev -w server"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
